import type { Metadata } from "next";
import { Eye, Globe2, ShieldCheck } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { SectionHeading } from "@/components/ui";

export const metadata: Metadata = { title: "About Us", description: "Learn about TitleCheckPro and its clear country-specific vehicle report request experience.", alternates: { canonical: "/about" } };
export default function AboutPage() { return <><PageHero title="Clarity before commitment" description="TitleCheckPro is a vehicle report request platform designed around transparent forms, straightforward packages and country-specific details." /><section className="section"><div className="container grid items-center gap-12 lg:grid-cols-2"><div><SectionHeading eyebrow="Our approach" title="A calmer way to request vehicle information" description="Buying or selling a used vehicle already involves enough moving parts. Our interface keeps the report request process focused and understandable." /></div><div className="grid gap-4">{[[Eye, "Clear by design", "Plain-language forms and package comparisons."], [Globe2, "Local context", "Dedicated workflows for Australia, Canada and the United States."], [ShieldCheck, "Honest boundaries", "No fabricated ratings, provider claims or guaranteed record availability."]].map(([Icon, title, text]) => { const C = Icon as typeof Eye; return <article className="rounded-2xl bg-[#F5F5F5] p-6" key={String(title)}><C className="text-[#b48800]" /><h2 className="mt-3 font-heading text-xl font-black">{String(title)}</h2><p className="mt-2 text-sm text-[#666]">{String(text)}</p></article>; })}</div></div></section></>; }
